/*var saveVar = "Level1";
private var saveString;
private var newPrefab;
private var amount = 1;
*/
function Update () {
	/*
	if(Input.GetKeyDown ("b")){
	saveString = "Assets/Made/Custom Levels/" + saveVar + amount + ".prefab";
	newPrefab = CreateEmptyPrefab(saveString);
	ReplacePrefab(transform.gameObject, newPrefab, ReplacePrefabOptions.ConnectToPrefab);
	amount = amount + 1;
	}
	*/
}